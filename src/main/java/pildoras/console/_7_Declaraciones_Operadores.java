package pildoras.console;

public class _7_Declaraciones_Operadores {

    /**
     * CONSTANTES EN JAVA
     * Espacio en la memoria del ordenador donde se almacenara un valor que no podra cambiar durante la ejecucion del programa
     *
     * Se usa a la hora de crear un pprogama a veces surge la necesidadd de guardar datos temporales que necesitaras utilizar
     * en el futuro en ese mismo programa. Dichos datos deberan ser fijos
     *
     * final double pulgadas = 2,54
     * @param args
     */

    /**
     * OPERADORES JAVA
     *
     * Aritmeticos (+, -, *, /)
     * Logicos
     *      * > Mayor que
     *      * < menor que
     *      * <> mayor o menor que
     *      * != distinto que
     *      * == igual que
     *      * && y logico
     *      * || o logico
     * Incremento: ++
     * Decremento: --
     * Concatenacion: +:
     * @param args
     */
    public static void main(String[] args) {
        int a = 5;
        int b;

        b=7;
        int c = b+a;
        c++;
        System.out.println(c);
        double d = 10;
        System.out.println(d);
    }
}
